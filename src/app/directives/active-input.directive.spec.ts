import { ActiveInputDirective } from './active-input.directive';

describe('ActiveInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveInputDirective();
    expect(directive).toBeTruthy();
  });
});
